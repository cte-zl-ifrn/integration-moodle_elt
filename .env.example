# ============================================
# Moodle Configuration
# ============================================
# You can configure Moodle instances in two ways:
#
# OPTION 1: Comma-separated lists (Recommended for multiple instances)
# - Provide comma-separated URLs and tokens
# - URLs will be automatically validated to use https://
# - Number of URLs must match number of tokens
#
# Example:
# MOODLE_URLS=https://moodle1.example.com,https://moodle2.example.com,https://moodle3.example.com,https://moodle4.example.com
# MOODLE_TOKENS=token1_here,token2_here,token3_here,token4_here

MOODLE_URLS=https://moodle1.example.com,https://moodle2.example.com,https://moodle3.example.com,https://moodle4.example.com
MOODLE_TOKENS=YOUR_TOKEN1_HERE,YOUR_TOKEN2_HERE,YOUR_TOKEN3_HERE,YOUR_TOKEN4_HERE

# OPTION 2: Individual instance variables (Legacy - still supported)
# - Configure each Moodle instance separately
# - Useful if you have only one or two instances
# - If both formats are provided, individual variables take precedence

# Moodle Instance 1 Configuration
moodle1_url=https://moodle1.example.com
moodle1_token=YOUR_MOODLE1_TOKEN_HERE

# Moodle Instance 2 Configuration
moodle2_url=https://moodle2.example.com
moodle2_token=YOUR_MOODLE2_TOKEN_HERE

# Moodle Instance 3 Configuration
moodle3_url=https://moodle3.example.com
moodle3_token=YOUR_MOODLE3_TOKEN_HERE

# Moodle Instance 4 Configuration (On-Premise - Use SQL Direct if needed)
moodle4_url=https://moodle4.example.com
moodle4_token=YOUR_MOODLE4_TOKEN_HERE

# ============================================
# Airflow Configuration
# ============================================
AIRFLOW_UID=50000
AIRFLOW_PROJ_DIR=.

# Airflow Web UI Credentials
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin

# PostgreSQL Configuration
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Superset Configuration
SUPERSET_SECRET_KEY=change_this_to_a_random_secret_key_for_production
